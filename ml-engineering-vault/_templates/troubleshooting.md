---
type: troubleshooting
title: "<% tp.file.title %>"
libs:
  - "<% await tp.system.suggester(['pandas', 'pytorch', 'scikit-learn', 'matplotlib', 'seaborn', 'statsmodels', 'spacy', 'nltk', 'xgboost', 'catboost', 'numpy', 'polars', 'uv', 'other'], ['pandas', 'pytorch', 'scikit-learn', 'matplotlib', 'seaborn', 'statsmodels', 'spacy', 'nltk', 'xgboost', 'catboost', 'numpy', 'polars', 'uv', 'other']) %>"
tags:
  - "<% await tp.system.suggester(['error/runtime', 'error/type', 'error/shape', 'error/memory', 'error/import', 'error/cuda', 'error/convergence', 'error/install', 'error/performance', 'error/other'], ['error/runtime', 'error/type', 'error/shape', 'error/memory', 'error/import', 'error/cuda', 'error/convergence', 'error/install', 'error/performance', 'error/other']) %>"
related:
  - "[[]]"
created: <% tp.date.now("YYYY-MM-DD") %>
updated: <% tp.date.now("YYYY-MM-DD") %>
---

# <% tp.file.title %>

> What area of breakage does this note cover?

---

## Error Description

**Error:**
```
paste error message here
```

**Context:** When does this happen?

**Root Cause:** Why it happens.

**Fix:**
```python

```

**Prevention:** How to avoid it.

---

<!-- Copy the section above to add more errors to this topic -->
